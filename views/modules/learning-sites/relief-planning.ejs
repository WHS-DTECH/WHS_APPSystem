<%- include('../../partials/header') %>

<section class="page-title">
  <p class="eyebrow">Teacher / ADMIN module</p>
  <h1>Relief Planning</h1>
  <p class="role-line">Signed in through the unified Google account as <strong><%= activeRole %></strong>.</p>
</section>

<section class="panel">
  <h2>Google access is connected</h2>
  <p>The legacy access key is no longer required for this entry point. The calendar data import will be moved into PostgreSQL before this page replaces the legacy Relief Plan calendar.</p>
  <a class="button button-primary" href="/learning-sites">Back to Learning Sites</a>
</section>

<%- include('../../partials/footer') %>
